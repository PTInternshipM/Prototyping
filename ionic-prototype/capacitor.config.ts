import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.organization.ionicPrototype',
  appName: 'IonicPrototype',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
